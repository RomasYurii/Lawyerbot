**Lawyerbot**

Deploy 

    git clone https://github.com/RomasYurii/Lawyerbot.git
    cd Lawyerbot

Create .env file:

    .env.example .env

Запустіть проект за допомогою Docker:

    docker-compose up -d --build